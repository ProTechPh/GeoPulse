# Pre-Deployment Security Checklist

## ⚠️ CRITICAL: Before Deploying to Render

### Security Items to Check

- [x] **`.env` files are in `.gitignore`** ✅ Already secure
- [ ] **Generate NEW JWT_SECRET for production** 
  - Current: `your-super-secret-jwt-key-change-this-in-production` ❌
  - Action needed: Generate random string for production
  - You can use: https://www.lastpass.com/features/password-generator (64 characters)

### Environment Variables to Update in Render

All of these will be added to Render dashboard (NOT in code):

```env
# Critical - Must be changed from default
JWT_SECRET=<generate-new-random-string-here>

# Database - Already configured
MONGODB_URI=mongodb+srv://jerickogarcia0_db_user:****@geopulse.p5hbbla.mongodb.net/geopulse

# ImageKit - Already configured
IMAGEKIT_PUBLIC_KEY=public_VWMrlSx4wBDKYGNfGUdq+rxg3Mw=
IMAGEKIT_PRIVATE_KEY=private_0LfHHAZv9MWnoQR7zU8if4wSDAY=
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/protechworks/

# Email - Already configured
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=jerickogarcia0@gmail.com
SMTP_PASS=jlda vyjh aefm lobw

# Frontend URL - MUST UPDATE
CLIENT_URL=https://YOUR-FRONTEND-URL.vercel.app
```

### What NOT to Do

❌ **DON'T** commit `.env` files to GitHub (already protected by `.gitignore`)  
❌ **DON'T** use development JWT_SECRET in production  
❌ **DON'T** hardcode secrets in code  

### What TO Do

✅ **DO** generate new JWT_SECRET  
✅ **DO** use Render's environment variables feature  
✅ **DO** test the `/api/health` endpoint after deployment  
✅ **DO** verify MongoDB Atlas IP whitelist allows all (0.0.0.0/0)  

## Quick JWT Secret Generator

Run this command to generate a secure JWT secret:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Or use an online generator:
- https://www.lastpass.com/features/password-generator
- Generate 64 character string
- Copy and use as JWT_SECRET

---

After completing this checklist, proceed to [DEPLOY_STEPS.md](./DEPLOY_STEPS.md)

